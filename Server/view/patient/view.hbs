<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Patients Data</title>
        <link href="css/dashboard.css" rel="stylesheet">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="canonical" href="https://getbootstrap.com/docs/5.1/examples/dashboard/">


    <!-- Bootstrap core CSS -->
<link href="css/bootstrap.min.css" rel="stylesheet">

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
      .infoBoxUserData{
        background-color: aliceblue;
        padding: 1.5rem;
      }
      .titlesA{
        padding-left: 1.2rem;
        line-height: 6px;
        padding-top: 5px;
      }
      .addPaddingToView{
        padding-top: 100px;
      }
    </style>

</head>
<body class="container">
          <h2 class="addPaddingToView" id="viewPatientsData">Patients Data</h2>
       <div class="table-responsive">
        <table class="table table-striped table-sm">
          <thead>
            <tr>
              <th scope="col">Identity</th>
              <th scope="col">Email Address</th>
              <th scope="col">First Name</th>
              <th scope="col">Family Name</th>
              <th scope="col">Patients</th>
              <th scope="col">Documents</th>
              <th scope="col">Active</th>
            </tr>
          </thead>
          <tbody class="text-right" id="updatePatientsDataPagenation">
              {{#each patientsData.results}}
            <tr>
                <td>{{this.identity}}</td>
              <td>{{this.emailAddress}}</td>
              <td>{{this.firstName}}</td>
              <td>{{this.familyName}}</td>
              <td>
                              <ul >
                  {{#each this.doctors}}
                       <li style="list-style-type: none;">{{this}}</li>
                    {{/each}}   
              </ul>
              </td>
             <td>
                 <ul >
                  {{#each this.docs}}
                       <li style="list-style-type: none;"><a href="{{this.url}}">{{this.name}}</a></li>
                    {{/each}}   
              </ul>

              </td>
                              {{#if this.isActive}}
              <td>
                    <p class="text-success">{{this.isActive}}</p>            
              </td>
              {{else}}
               <td>

                    <p class="text-danger">{{this.isActive}}</p>
             
               </td>

                {{/if}}
            </tr>
              {{/each}}
          </tbody>
        </table>
      </div>


      <nav aria-label="Page navigation example">
  <ul class="pagination">
    {{#if patientsData.previous}}
    <li class="page-item"><a class="page-link" href="?page={{patientsData.previous.page}}&limit=5">Previous</a></li>
    {{/if}}
    {{#each patientsData.pages}}
        <li class="page-item"><a class="page-link" href="?page={{{this}}}&limit=5">{{{this}}}</a></li>
    {{/each}}
     {{#if patientsData.next}}
    <li class="page-item"><a class="page-link" href="?page={{patientsData.next.page}}&limit=5">Next</a></li>
    {{/if}}
  </ul>
</nav>

</body>
</html>